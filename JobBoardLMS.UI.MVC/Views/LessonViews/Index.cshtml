@model IEnumerable<JobBoardLMS.DATA.EF.LessonView>

@{
    ViewBag.Title = "Lesson Views";
}
<div class="site-section ftco-subscribe-1 site-blocks-cover pb-4" style="background-image: url('../../Content/images/bg_1.jpg')">
    @* class="site-section ftco-subscribe-1" *@
    <div class="container">
        <div class="row align-items-end">
            <div class="col-lg-7">
                <h2 class="mb-0">@ViewBag.Title</h2>
                <p>Lorem ipsum dolor sit amet consectetur adipisicing.</p>
            </div>
        </div>
    </div>
</div>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateViewed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lessons.LessonTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateViewed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Lessons.LessonTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LessonViewID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LessonViewID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LessonViewID })
        </td>
    </tr>
}

</table>
